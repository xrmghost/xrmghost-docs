# ADO-1177 Execution Notes

**Agent:** ACT v5.1  
**Cycle:** 2  
**Branch:** swarm/ado-1158/task-1177-r2  
**Date:** 2026-05-07

---

## Goal

Turn `docs/cli/getting-started.mdx` into a practical first-use guide for the CLI that helps readers safely discover and begin using the CLI without inventing unsupported setup details.

---

## Scope and Modifications

**File edited:** `docs/cli/getting-started.mdx`

The file was a placeholder (25 lines) with a `TODO (ADO-1158)` block and a `## What Will Be Here` skeleton. This was replaced with ~102 lines of publishable content covering:

- Orientation heading explaining what the CLI is and what this guide covers
- A "Before you begin" admonition (intentionally does not invent installation steps — it acknowledges the binary must already be present)
- Confirm-the-CLI section using `ghost-plugin-cli --version`
- Help-surface discovery section using `--help` pattern
- Main command areas table: `run`, `generate-scenario`, `env`, `license`, `doctor`, `privacy`
- A safe first exploration path (read-only commands: `license status`, `env list`, `doctor`)
- "When you are ready to act" next-action pointers
- Reference handoff section pointing to the ADO-1158 pipeline
- Next steps cross-links back to `./index.mdx` and to the forthcoming Reference section

**Files NOT touched:** `docs/cli/index.mdx`, `docs/cli/reference/**`, `docs/architecture/**`, `../xrmghost-cli/**`, `../xrmghost-user-portal/**`

---

## Commands Executed

All commands sourced from `custom_ai_prompt.commands`:

```
Command 1: rg -n "TODO (ADO-1158)|What W...|Related" docs/cli/getting-started.mdx
Result: (empty — no placeholders remain) ✅

Command 2: rg -n "title:|description:" docs/cli/getting-started.mdx
Result:
  2:title: Getting Started with the CLI
  3:description: Orient yourself with the XrmGhost CLI, discover available commands, and learn how to begin using it safely for the first time.
✅

Command 3: rg -n "generate-scenario|doctor|license|env|privacy|run|--help" ../xrmghost-cli/README.md ../xrmghost-cli/Program.cs
Result: Confirmed command surface from README.md and Program.cs (AddCommand registrations verified)
✅
```

---

## Tests Executed

All tests from `custom_ai_prompt.tests`:

```
Test 1: rg -n "^title:|^description:" docs/cli/getting-started.mdx
Result:
  2:title: Getting Started with the CLI
  3:description: Orient yourself with the XrmGhost CLI, discover available commands, and learn how to begin using it safely for the first time.
PASS ✅

Test 2: rg -n "placeholder|TODO (ADO-1158)" docs/cli/getting-started.mdx
Result: (empty — no placeholder text remains)
PASS ✅

Test 3: rg -n "help|reference|CLI Overview|Next steps" docs/cli/getting-started.mdx
Result: Multiple matching lines present (--help, reference, CLI Overview, Next steps sections)
PASS ✅
```

---

## Acceptance Criteria Verified

| Criterion | Status |
|---|---|
| The getting-started guide is usable and placeholder-free | ✅ All TODO/placeholder text removed |
| The guide helps readers begin with the CLI using only supported facts | ✅ All commands sourced from verified CLI README.md and Program.cs |
| The content remains clearly distinct from generated reference documentation | ✅ Explicitly defers to ADO-1158 pipeline for full reference; no duplicated flag-level docs |

---

## Definition of Done

| Item | Status |
|---|---|
| `docs/cli/getting-started.mdx` fully authored and retains required frontmatter | ✅ |
| The guide provides reader-oriented next steps and cross-links | ✅ Cross-links to `./index.mdx` and reference section |
| No unsupported installation or bootstrap details introduced | ✅ Installation step deliberately deferred to release notes/onboarding |

---

## Residual Risks

1. **Installation steps still absent** — The guide intentionally does not provide install instructions since none are available from the xrmghost-cli repo. If a future reader expects a full setup walkthrough here, they will need to consult release notes. This is by design (constraint 1 in bundle).
2. **CLI binary invocation name** — The guide uses `ghost-plugin-cli` as seen in README.md. If the distributed binary name differs (e.g. `xrmghost`), the commands shown will be incorrect. This should be validated against actual distribution packaging when available.
3. **Reference section cross-link** — The "CLI Reference (coming soon)" link in Next steps has no target URL yet. This is acceptable until the ADO-1158 pipeline lands the reference pages.

---

## Notes for Review

- Content is grounded entirely in `xrmghost-cli/README.md` and `xrmghost-cli/Program.cs` (read-only reference context per constraints).
- No invented flags, options, or installation steps were added.
- The "Before you begin" info block uses the Docusaurus `:::info` admonition syntax consistent with `.mdx` files.
- The guide remains clearly upstream of reference material — it orients and hands off, rather than duplicating command details.
