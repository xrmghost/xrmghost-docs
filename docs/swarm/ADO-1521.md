# ADO-1521

## Goal

Publish the minimal canonical go-live documentation cluster in `xrmghost-docs` so public users can understand XrmGhost and sibling repos can link to stable docs targets before ADO-1382 editorial close-out.

## Scope and changes

Implemented only the requested docs slice in `src/content/docs/`:

- updated `src/content/docs/index.mdx` to surface the canonical public docs cluster
- updated `src/content/docs/getting-started.mdx` to route readers to the new product-level pages
- added `src/content/docs/overview/index.mdx`
- added `src/content/docs/how-it-works/index.mdx`
- added `src/content/docs/editions/index.mdx`

No other product repos or docs paths were modified. Editorial polishing and broader harmonization were intentionally left to ADO-1382.

## Commands executed

1. `python3 - <<'PY' ...` — exit 0  
   Loaded and validated the task bundle structure and prompt contents.
2. `git status --short` — exit 0  
   Verified initial clean working state.
3. `ls "src/content/docs"` — exit 0  
   Confirmed docs parent path before creating new pages.
4. `ls` — exit 0  
   Confirmed repo root before creating `docs/swarm/` receipt.
5. `npm install && npm run build && rg -n "\bFree\b" ...` — partial success / final exit non-zero  
   `npm install` succeeded. `npm run build` succeeded. Final `rg` step failed because `rg` is not installed in this environment.
6. MCP grep search for `\bFree\b` under `src/content/docs` — success  
   Confirmed no deprecated `Free` term in the docs content set.
7. `git status --short` — exit 0  
   Verified only in-scope doc files changed.

## Acceptance criteria verification

- **Canonical go-live product pages exist under `src/content/docs/`.** Verified by added `/overview/`, `/how-it-works/`, and `/editions/` pages.
- **Existing public entry points link to the new canonical docs targets.** Verified in `index.mdx` and `getting-started.mdx`.
- **Content is English-only and uses Trial / Standard terminology consistently.** Verified in authored pages and by absence of deprecated `Free` terminology.
- **Docs slice is complete enough that ADO-1382 can stay editorial-only.** Verified: this task added the missing product-level canonical pages and discoverability links without expanding into style-polish work.

## Test results

- `npm run build` — passed.
- Generated site routes included:
  - `/overview/index.html`
  - `/how-it-works/index.html`
  - `/editions/index.html`
- Link discoverability was validated through the updated entry-point pages plus successful static route generation.

## Residual risks

- Product naming or packaging changes could require later copy adjustments to these canonical pages.
- Entry-point wording is intentionally minimal to avoid absorbing ADO-1382 editorial cleanup.

## Review notes

- The new pages are intentionally canonical and concise, focused on public orientation rather than detailed implementation guidance.
- `docs/swarm/ADO-1521.md` was added as required by the ACT workflow and sits outside the content slice only for execution reporting.
